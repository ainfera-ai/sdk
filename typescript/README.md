# ainfera — TypeScript SDK

The TypeScript SDK ships D30 (AIN-44). The Python SDK is the D9 launch deliverable — see [`../python/`](../python/) and [`pip install ainfera`](https://pypi.org/project/ainfera/).
